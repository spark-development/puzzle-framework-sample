"use strict";

/** Session configuration. */
module.exports = {
  /**
   * The secret used to encrypt the session cookie.
   *
   * @type {string}
   */
  secret: 'qwertyuiop',

  /**
   * The secret used to encrypt the session cookie.
   *
   * @type {string}
   */
  store: 'memory'
};
